# Aman Sharma — Job Ready Full-Stack Portfolio

Two recruiter-focused full-stack applications:

- `real-time-social-platform` — React + Node.js/Express + MongoDB + Socket.IO
- `clinic-management-saas` — React + Node.js/Express + PostgreSQL + Socket.IO

These implementations are original portfolio implementations inspired by common production SaaS patterns. Do not commit secrets; use `.env.example` files.
